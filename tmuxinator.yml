name: sharon-portfolio
root: ~/Work/P/sharon-portfolio

windows:
  - services:
      layout: main-horizontal
      panes:
        - jekyll serve --host 0.0.0.0
        - sass --watch _sass:css
  - bash:
  - root:
    - vim
  - sass:
    - cd _sass
    - vim
